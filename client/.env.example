# Public base URL of the backend API.
# - Web (served by the backend on the same origin): leave EMPTY -> uses relative /api
# - Android app (Capacitor) or web hosted on a different domain: set the full API URL
#
# Example for the Android release build:
#   VITE_API_BASE=https://api.umfaris.example.com
VITE_API_BASE=
